#include "main.h"
#include <stdio.h>

/**
 * main - Prints the number of args
 * @argc: argmeument counter
 * @argv: argument vector
 *
 * Return: zero
 */

int main(int argc, char **argv)
{
(void)argv;
printf("%d\n", argc);
return(0);
}
